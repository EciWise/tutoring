/**
 * Punto único de importación del cliente Prisma generado.
 *
 * El generador `prisma-client` (Prisma 7) emite a `generated/prisma` fuera de
 * `src`; esta es la ÚNICA ruta relativa profunda hacia esa carpeta. Todo el
 * código de infraestructura debe importar `PrismaClient`, el namespace `Prisma`
 * y los enums desde aquí, no desde la ruta generada.
 */
export * from '../../../../generated/prisma/client';
